Полный гайд развёртывания

## Оглавление
1. [Требования](#требования)
2. [Подготовка Telegram](#подготовка-telegram)
3. [Настройка конфигурации](#настройка-конфигурации)
4. [Поднятие базы данных](#поднятие-базы-данных)
5. [Установка и запуск проекта](#установка-и-запуск-проекта)
6. [Решение проблем](#решение-проблем)

---

## Требования

Убедись, что на ПК установлено:

- **JDK 17** или выше
  - Проверь: `java -version`
  - Скачать: https://www.oracle.com/java/technologies/javase-jdk17-downloads.html

- **Maven 3.8** или выше
  - Проверь: `mvn -version`
  - Скачать: https://maven.apache.org/download.cgi

- **Git** (для клонирования репозитория)
  - Проверь: `git --version`

- **MySQL 8.0** (локально) **или** **Docker** (для контейнера MySQL)
  - Если выберешь Docker: установи Docker Desktop (https://www.docker.com/products/docker-desktop)
  - Если локально: скачай с https://dev.mysql.com/downloads/mysql/

- **Интернет** (для скачивания зависимостей Maven и обращения к Telegram API)

---

## Подготовка Telegram

### Шаг 1: Создать бота через BotFather

1. Открой Telegram и найди контакт **@BotFather** (официальный бот для создания ботов)
2. Отправь команду `/newbot`
3. Введи имя бота (например, `MyGameBot`)
4. Введи username бота (должен заканчиваться на `bot`, например, `MyGameBot_bot`)
5. BotFather отправит **TOKEN** вида: `123456789:ABCdefGhIjKlmnOpQrStUvWxYzAbCdEfGhI`

**Сохрани эти значения:**
- `BOT_TOKEN` (длинная строка)
- `BOT_NAME` (username без @)

---

## Настройка конфигурации

### Шаг 1: Клонировать репозиторий

```bash
git clone https://github.com/BogPov/tgBot
cd tgBot
```

### Шаг 2: Создать application.properties

1. Открой папку проекта: `src/main/resources/`
2. Создай файл `application.properties`
3. Заполни содержимое:

```properties
bot.token=ВСТАВЬ ТОКЕН БОТА
bot.name=ВСТАВЬ ИМЯ БОТА
```

**Пример:**

```properties
bot.token=123456789:ABCdefGhIjKlmnOpQrStUvWxYzAbCdEfGhI
bot.name=MyGameBot_bot
```

**Важно:** не используй @ перед именем бота.

### Шаг 3 (рекомендуется): Использовать .env файл

Вместо того, чтобы экспортить переменные окружения руками, можно создать `.env` файл в корне проекта:

**Создай файл `.env` в корне проекта (рядом с `pom.xml`):**

```bash
DB_URL=
DB_USERNAME=
DB_PASSWORD=
```

Затем перед запуском загрузи переменные:

**Linux / macOS / WSL:**
```bash
export $(cat .env | xargs)
```

**Windows (PowerShell):**
```powershell
Get-Content .env | ForEach-Object {
  if ($_ -match "^([^=]+)=(.+)$") {
    [Environment]::SetEnvironmentVariable($matches[1], $matches[2], "Process")
  }
}
```

---

## Поднятие базы данных

### Вариант A: MySQL в Docker (рекомендуется)

**Шаги:**

1. Убедись, что Docker запущен
2. Скачай образ и запусти контейнер:

```bash
docker run --name -NAME \
  -e MYSQL_ROOT_PASSWORD=пароль для рута в mysql \
  -e MYSQL_DATABASE=имя бд \
  -p 3306:3306 \
  -d mysql:8.0
```

3. Проверь, что контейнер запущен:

```bash
docker ps
```

4. Проверь подключение:

```bash
mysql -h localhost -u root -p
# введи пароль
# затем выполни:
SHOW DATABASES;
```
---

### Вариант B: MySQL локально на ПК

**Если MySQL уже установлен на ПК:**

1. Запусти MySQL сервис (зависит от ОС):

   **Windows:**
   ```bash
   net start MySQL80
   ```

   **Linux:**
   ```bash
   sudo systemctl start mysql
   ```

   **macOS:**
   ```bash
   mysql.server start
   ```

2. Создай базу данных:

```bash
mysql -u root -p
# введи пароль
# затем выполни:
CREATE DATABASE ИМЯ БД;
EXIT;
```

3. Таблица `players` создастся автоматически при первом запуске приложения.

---

## Установка и запуск проекта

### Шаг 1: Настроить переменные окружения БД

Приложение берёт параметры подключения к БД из переменных окружения.

**Важно:** `DB_URL` должен быть **без** префикса `jdbc:`, потому что в коде префикс добавляется автоматически.

**Linux / macOS / WSL:**
```bash
export DB_URL=
export DB_USERNAME=
export DB_PASSWORD=
```

**Windows (cmd):**
```cmd
set DB_URL=
set DB_USERNAME=
set DB_PASSWORD=
```

**Windows (PowerShell):**
```powershell
$env:DB_URL=
$env:DB_USERNAME=
$env:DB_PASSWORD=
```

---

### Шаг 2: Собрать проект

```bash
mvn clean package
```

Команда:
- Очищает старые файлы (`clean`)
- Скачивает зависимости
- Компилирует исходный код
- Собирает JAR-файл

При успехе в конце ты увидишь: `BUILD SUCCESS`

---

### Шаг 3: Запустить приложение

**Через Maven (проще для разработки):**

```bash
mvn spring-boot:run
```

**Или запустить JAR напрямую:**

```bash
java -jar target/tgBot-0.0.1-SNAPSHOT.jar
```

(название JAR может отличаться; проверь в папке `target/`)

**При успешном запуске в консоли ты увидишь:**
```
... : Started Application in X.XXX seconds
```

Приложение запущено и готово принимать сообщения от Telegram.

---


## Решение проблем

### Проблема: "Could not find a valid Docker environment"

**Причина:** Testcontainers (в тестах) не может найти Docker.

**Решение:**
- Убедись, что Docker запущен: `docker ps`
- Если Docker не установлен → скачай Docker Desktop
---

### Проблема: "Connection refused" при подключении к БД

**Причина:** MySQL не запущен или неправильно выставлены переменные окружения.

**Решение:**
1. Проверь, что MySQL запущен:
   ```bash
   docker ps
   # или для локального MySQL:
   mysql -u root -p
   ```

2. Проверь переменные окружения:
   ```bash
   echo $DB_URL
   echo $DB_USERNAME
   echo $DB_PASSWORD
   ```

3. Убедись, что `DB_URL` **без** `jdbc:` (например: `mysql://localhost:3306/festena`)

---

### Проблема: Бот не отвечает

**Причина:** Неправильный `bot.token` или `bot.name` в `application.properties`.

**Решение:**
1. Проверь, что `src/main/resources/application.properties` существует
2. Проверь, что `bot.token` и `bot.name` заполнены правильно (без лишних пробелов)
3. Перезапусти приложение: `mvn spring-boot:run`
4. Проверь логи в консоли на ошибки

---


### Проблема: Maven не находится

**Причина:** Maven не установлен или не добавлен в PATH.

**Решение:**
1. Скачай Maven: https://maven.apache.org/download.cgi
2. Распакуй и добавь папку `bin/` в PATH:
   - **Windows:** переменные окружения системы
   - **Linux/macOS:** добавь в `~/.bashrc` или `~/.zshrc`:
   ```bash
   export PATH=$PATH:/path/to/apache-maven-3.x.x/bin
   ```

3. Проверь: `mvn -version`

---

### Проблема: JDK не находится

**Причина:** Java не установлена или не добавлена в PATH.

**Решение:**
1. Скачай JDK 17+: https://www.oracle.com/java/technologies/javase-downloads.html
2. Установи и добавь в PATH
3. Проверь: `java -version`

---


## Заключение

Если после выполнения всех шагов бот работает и отвечает на сообщения в Telegram — развёртывание успешно! 🎉

При возникновении вопросов или ошибок — смотри раздел [Решение проблем](#решение-проблем) или проверь логи приложения в консоли.

**Успехов!** 🚀
